import { LeaseRenewal } from '../../types';

/**
 * Types specifically for the RenewalGrid component.
 * TODO: Define Props, Row, and Column interfaces.
 */
export interface RenewalGridProps {
    data: LeaseRenewal[];
}
